{% extends '@OHMediaBackend/base.html.twig' %}

{% block meta_title %}Form Fields{% endblock %}

{% block breadcrumbs %}
  {{ bootstrap_breadcrumbs(
    bootstrap_breadcrumb(bootstrap_icon('ui-checks-grid') ~ ' Form Fields', 'form_field_index'),
  ) }}
{% endblock %}

{% set can_create_form_field = is_granted(attributes.create, new_form_field) %}

{% set create_form_field_href = path('form_field_create') %}

{% block actions %}
  {% if can_create_form_field %}
    <a href="{{ create_form_field_href }}" class="btn btn-sm btn-primary">
      {{ bootstrap_icon('plus') }} Add Form Field
    </a>
  {% endif %}
{% endblock %}

{% block main %}
  {% set form_field_count = form_fields|length %}

  <div class="card">
    <div class="card-body">
      <div class="card-title card-title-with-count">
        <h1 class="card-title-heading">Form Fields</h1>
        <div class="card-title-count">
          {{ bootstrap_badge_primary(form_field_count) }}
        </div>
      </div>

      {% if form_field_count %}
        <table class="table table-striped">
          <thead>
            <tr>
              <th style="width:1rem">&nbsp;</th>
              <th>Form Field</th>
              <th>Last Updated</th>
              <th></th>
            </tr>
          </thead>
          <tbody
            data-sortable
            data-sortable-csrf-name="{{ csrf_token_name }}"
            data-sortable-csrf-token="{{ csrf_token(csrf_token_name) }}"
            data-sortable-url="{{ path('form_field_reorder_post') }}"
          >
          {% for form_field in form_fields %}
            <tr data-id="{{ form_field.id }}">
              <td data-handle>{{ bootstrap_icon('arrows-move') }}</td>
              <td>{{ form_field }}</td>
              <td>{{ form_field.updatedAt|datetime }}</td>
              <td>
                {% if is_granted(attributes.edit, form_field) %}
                  <a class="btn btn-sm btn-primary btn-action" href="{{ path('form_field_edit', {id: form_field.id}) }}" title="Edit">
                    {{ bootstrap_icon('pen-fill') }}
                    <span class="visually-hidden">Edit</span>
                  </a>
                {% endif %}

                {% if is_granted(attributes.delete, form_field) %}
                  <a class="btn btn-sm btn-danger btn-action" href="{{ path('form_field_delete', {id: form_field.id}) }}" title="Delete" data-confirm="Are you sure you want to delete this form field? Clicking OK will take you to a verification step to delete this entry.">
                    {{ bootstrap_icon('trash-fill') }}
                    <span class="visually-hidden">Delete</span>
                  </a>
                {% endif %}
              </td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
      {% else %}
        <p>
          No form fields found.
          {% if can_create_form_field %}
            <a href="{{ create_form_field_href }}">
              Click here to add a form field.
            </a>
          {% endif %}
        </p>
      {% endif %}
    </div>
  </div>
{% endblock %}
