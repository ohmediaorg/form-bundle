{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Delete ' ~ form_field %}

{% block breadcrumbs %}
  {{ bootstrap_breadcrumbs(
    bootstrap_breadcrumb(bootstrap_icon('ui-checks-grid') ~ ' Form Fields', 'form_field_index'),
    bootstrap_breadcrumb('Delete'),
  ) }}
{% endblock %}
