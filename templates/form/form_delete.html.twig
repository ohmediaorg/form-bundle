{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Delete ' ~ form_entity %}

{% block breadcrumbs %}
  {{ bootstrap_breadcrumbs(
    bootstrap_breadcrumb(bootstrap_icon('ui-checks') ~ ' Forms', 'form_index'),
    bootstrap_breadcrumb(form_entity, 'form_view', {id: form_entity.id}),
    bootstrap_breadcrumb('Delete'),
  ) }}
{% endblock %}
